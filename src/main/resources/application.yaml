spring:
  application:
    name: travel_agent

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/travel_agent
    username: postgres
    password: password

  data:
    redis:
      host: localhost
      port: 6379